/**
 * description: 첫 화면 UI
 * render 함수가 실행되고 나면 전체 페이지 보여줌.
 */

class StartView {
  constructor() {
    //DOM cacheing
  }

  render(data) {
    //데이터 받아와서 템플릿 완성 시키고 mainContents DOM에 넣고
    //화면 보여주기.
  }
}
